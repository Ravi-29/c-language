#include<stdio.h>

int main()
{
    // int a,b;
     float sum;
    // scanf("%d, %d", &a,&b);
    // printf("the sum of a+b is %d\n", a+b);
    // printf("power of a and b is %f\n", pow(a,b));

    sum = 5/2;
    printf("the value of sum is %f\n", sum);

    return 0;
}